<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Ejemplo_Layout.Pages.StackLayoutPage"
             Title="StackLayoutPage">
    
    <StackLayout Spacing="10" Margin="10">
        <BoxView BackgroundColor="#ADCBFF" HeightRequest="100" WidthRequest="100"
                 HorizontalOptions="Start"/>
        <BoxView BackgroundColor="#CCFFAD" HeightRequest="100" WidthRequest="100"
                 HorizontalOptions="Center"/>
        <BoxView BackgroundColor="#ADCBFF" HeightRequest="100" WidthRequest="100"
                 HorizontalOptions="End"/>
    </StackLayout>

    <!--<Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*" 
          ColumnSpacing="10" RowSpacing="10">

        <BoxView Grid.Row="0" BackgroundColor="#ADCBFF" HeightRequest="100" WidthRequest="100"
                          HorizontalOptions="Start"/>
        <BoxView Grid.Row="1" BackgroundColor="#CCFFAD" HeightRequest="100" WidthRequest="100"
                          HorizontalOptions="Center" />
        <BoxView Grid.Row="2" BackgroundColor="#ADCBFF" HeightRequest="100" WidthRequest="100" 
                           HorizontalOptions="End"/>
        
    </Grid>-->

</ContentPage>